#!/usr/bin/env bash
# fault-rag — agent-facing entry point. Resolves to this repo regardless of cwd.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$ROOT"
exec python3 -m fault_rag "$@"
