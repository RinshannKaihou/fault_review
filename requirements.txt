# All are already present in the base image; pinned ranges are what was tested.
torch>=2.7,<3          # CPU build is fine
transformers>=5.14     # bge-m3 via AutoModel (XLM-R architecture)
huggingface_hub>=1.24  # snapshot_download for the model
numpy>=1.24
jieba>=0.42            # CJK tokenization for BM25
tqdm                   # embed progress bar
